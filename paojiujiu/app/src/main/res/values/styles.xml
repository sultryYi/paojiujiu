<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorSportHistoryBack</item>
        <item name="colorPrimaryDark">@color/colorSportHistoryBack</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="Custom"/>
    <style name="Custom.TabRadioButton">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:padding">5dp</item>
        <item name="android:gravity">center</item>
        <item name="android:button">@null</item>
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">@color/colorGrayText</item>
    </style>

    <!--自定义seekbarstyle-->
    <style name="CustomSeekBarAccuracyStyle" >
        <item name="android:maxHeight">30dp</item>
        <item name="android:indeterminateOnly">false</item>
        <item name="android:indeterminateDrawable">@color/colorAccuracy</item>
        <item name="android:progressDrawable">@drawable/seek_bar_accuracy_drawable</item>
        <item name="android:minHeight">30dp</item>
    </style>

    <style name="CustomSeekBarLevelStyle" >
        <item name="android:maxHeight">30dp</item>
        <item name="android:indeterminateOnly">false</item>
        <item name="android:indeterminateDrawable">@color/colorAccuracy</item>
        <item name="android:progressDrawable">@drawable/seek_bar_level_drawable</item>
        <item name="android:minHeight">30dp</item>
    </style>
</resources>
